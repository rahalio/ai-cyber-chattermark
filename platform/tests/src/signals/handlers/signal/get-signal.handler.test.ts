/**
 * GetSignal handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: signals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getSignal } from "@chattermark/api-server/domains/signals/handlers";
import type { SignalsDDDDependencies } from "@chattermark/api-server/domains/signals";
import { createSignal as createSignalEntity } from "@chattermark/factories/signals";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@chattermark/tests/shared";
import { SIGNALS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetSignal", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SignalsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SignalsDDDDependencies>(
      SIGNALS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createSignalEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetSignal, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { signalId: entity.id };

    await getSignal(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetSignal.execute).toHaveBeenCalledWith(expect.objectContaining({ signalId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { signalId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetSignal, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getSignal(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
