/**
 * ListScoringVersions handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: forecasts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listScoringVersions } from "@chattermark/api-server/domains/forecasts/handlers";
import type { ForecastsDDDDependencies } from "@chattermark/api-server/domains/forecasts";
import { createScoringVersion as createScoringVersionEntity } from "@chattermark/factories/forecasts";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@chattermark/tests/shared";
import { FORECASTS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListScoringVersions", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ForecastsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ForecastsDDDDependencies>(
      FORECASTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createScoringVersionEntity({ orgId }),
      createScoringVersionEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListScoringVersions, "execute").mockResolvedValue(useCaseResult);

    await listScoringVersions(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListScoringVersions.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
