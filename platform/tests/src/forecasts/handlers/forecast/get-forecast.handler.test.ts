/**
 * GetForecast handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: forecasts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getForecast } from "@chattermark/api-server/domains/forecasts/handlers";
import type { ForecastsDDDDependencies } from "@chattermark/api-server/domains/forecasts";
import { createForecast as createForecastEntity } from "@chattermark/factories/forecasts";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@chattermark/tests/shared";
import { FORECASTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetForecast", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ForecastsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ForecastsDDDDependencies>(
      FORECASTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createForecastEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetForecast, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { vulnerabilityId: entity.id };

    await getForecast(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetForecast.execute).toHaveBeenCalledWith(expect.objectContaining({ vulnerabilityId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { vulnerabilityId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetForecast, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getForecast(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
