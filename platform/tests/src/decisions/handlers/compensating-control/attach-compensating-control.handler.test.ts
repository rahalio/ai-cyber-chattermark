/**
 * AttachCompensatingControl handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: decisions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { attachCompensatingControl } from "@chattermark/api-server/domains/decisions/handlers";
import type { DecisionsDDDDependencies } from "@chattermark/api-server/domains/decisions";
import { createCompensatingControl as createCompensatingControlEntity } from "@chattermark/factories/decisions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@chattermark/tests/shared";
import { DECISIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("AttachCompensatingControl", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DecisionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DecisionsDDDDependencies>(
      DECISIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createCompensatingControlEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { decisionId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeAttachCompensatingControl, "execute").mockResolvedValue(useCaseResult);

    await attachCompensatingControl(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeAttachCompensatingControl.execute).toHaveBeenCalledWith(expect.objectContaining({ decisionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
