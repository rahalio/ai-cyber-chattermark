/**
 * ExecuteAssembleEvidentiaryTimeline - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: assurance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAssembleEvidentiaryTimeline Use Case
 *
 * DDD: Application use case for get EvidentiaryTimeline.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AssembleEvidentiaryTimelineInput, AssembleEvidentiaryTimelineOutput } from "../dto/evidentiary-timeline.dto";
import type { ExecutionContextService, IdGeneratorService } from "@chattermark/services/_shared/index.js";
import type { EvidentiaryTimelineRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteAssembleEvidentiaryTimeline {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly evidentiaryTimeline: EvidentiaryTimelineRepository
  ) {}

  async execute(input: AssembleEvidentiaryTimelineInput): Promise<AssembleEvidentiaryTimelineOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.asrId();

    // All field names extracted from OpenAPI spec: periodEnd, periodStart, purpose, vulnerabilityIds
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.evidentiaryTimeline.assembleEvidentiaryTimeline({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as AssembleEvidentiaryTimelineOutput;
  }
}
