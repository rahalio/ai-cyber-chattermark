/**
 * ExecuteSyncEstateItems - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: estate
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteSyncEstateItems Use Case
 *
 * DDD: Application use case for get EstateItem.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { SyncEstateItemsInput, SyncEstateItemsOutput } from "../dto/estate-item.dto";
import type { ExecutionContextService, IdGeneratorService } from "@chattermark/services/_shared/index.js";
import type { EstateItemRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteSyncEstateItems {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly estateItem: EstateItemRepository
  ) {}

  async execute(input: SyncEstateItemsInput): Promise<SyncEstateItemsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.estId();

    // All field names extracted from OpenAPI spec: items, sourceSystem
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.estateItem.syncEstateItems({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as SyncEstateItemsOutput;
  }
}
