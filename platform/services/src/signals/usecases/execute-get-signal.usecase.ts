/**
 * ExecuteGetSignal - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: signals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetSignal Use Case
 *
 * DDD: Application use case for get Signal.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetSignalInput, GetSignalOutput } from "../dto/signal.dto";
import type { ExecutionContextService, IdGeneratorService } from "@chattermark/services/_shared/index.js";
import type { SignalRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetSignal {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly signal: SignalRepository
  ) {}

  async execute(input: GetSignalInput): Promise<GetSignalOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.sigId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).signalId) {
      throw new ValidationError("signalId is required");
    }
    const signalId = (input as any).signalId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.signal.getSignal({ signalId: signalId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${signalId}`);
    }

    return entity as GetSignalOutput;
  }
}
