/**
 * ExecuteIngestSignal - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: signals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteIngestSignal Use Case
 *
 * DDD: Application use case for get Signal.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { IngestSignalInput, IngestSignalOutput } from "../dto/signal.dto";
import type { ExecutionContextService, IdGeneratorService } from "@chattermark/services/_shared/index.js";
import type { SignalRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteIngestSignal {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly signal: SignalRepository
  ) {}

  async execute(input: IngestSignalInput): Promise<IngestSignalOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.sigId();

    // All field names extracted from OpenAPI spec: platform, postedAt, sourceHandle, text, urls
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.signal.ingestSignal({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as IngestSignalOutput;
  }
}
